gatling {
  socket {
    connectTimeout = 2000
  }
  http {
    ahc {
      requestTimeout = 10000
      # Максимальное количество авто-повторов при сетевых сбоях (0 = отключено)
      maxRetry = 0
    }
  }
}
